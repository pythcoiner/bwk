name: CI
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

env:
  CARGO_TERM_COLOR: always

jobs:
  linter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: 1.88
          components: rustfmt, clippy
          override: true
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libudev-dev
      - name: rustfmt
        run: cargo fmt -- --check
      - name: clippy
        run: cargo clippy --all-targets -- -D warnings

  tests:
    needs: linter
    strategy:
      matrix:
        toolchain:
          - 1.85
          - nightly
        os:
          - ubuntu-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v1
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.toolchain }}
          override: true
          profile: minimal
      - name: Install system dependencies
        # gdb: lets the integration-test watchdog dump thread backtraces if a
        # scan ever hangs, so the job fails fast with a trace instead of stalling.
        run: sudo apt-get update && sudo apt-get install -y libudev-dev gdb
      - name: tests
        # Single-threaded: each integration test runs its own regtest daemons
        # (bitcoind/blindbit/electrsd); running them concurrently starves the
        # runner and causes flaky sync timeouts / wallet-state races.
        run: cargo test --features test --verbose --color always -- --nocapture --test-threads=1

  build:
    needs: tests
    strategy:
      matrix:
        toolchain:
          - 1.85
          - nightly
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v1
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.toolchain }}
          override: true
          profile: minimal
      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libudev-dev
      - name: build
        run: cargo build --release
